# Quad Central — BIM Coordination Highlights

Drop coordination images here. Each filename listed in `lib/data.ts`
under `projects[0].coordination[]` is resolved against this folder.

Currently expected:

- `bim-coordination.jpg` — the "Where Design Meets Function" collage
  (3D MEP model overview + as-built ceiling installation photos)

Recommended size: 1080 × 1620 px JPG, ~300–500 KB. Portrait orientation
works perfectly with the panel's max-h-[80vh] object-contain layout.

Until the image is added, the panel shows a placeholder with these
instructions and the rest of the project content still renders.
